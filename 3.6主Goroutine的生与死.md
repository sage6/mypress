<!--
 * @Date: 2026-08-12 21:21:56
 * @LastEditTime: 2026-08-12 21:37:25
 * @Description:
-->

## 3.6 主Goroutine的生与死

3.5里面schedinit把运行时的家底备起以后并不直接调用runtime.main,而是把它的入口地址压栈，
交给newproc造出第一个Goroutine,再由mstart启动调度循环，把这个Goroutine挑出来执行。

这只Goroutine与众不同。它是程序里诞生的第一个，承载着用户的main.main,也独自掌握着整个进程的生杀大权。
它一返回，进程就结束，哪怕别处有成千个Goroutine正忙。

### 3.6.1 runtime.main:用户代码之前的铺垫

运行时包里面的main函数（即runtime.main）与用户的main.main跑在同一个Goroutine上，但在把控制权交给用户
之前，它先料理几个只能由第一个Goroutine来做的事情。

```
// 主Goroutine入口
func main() {
    mp = getg().m

    // ...
    mainStarted = true  // 允许 newproc启动新的M

    if haveSysmon { // 启动系统监控线程
        systemstack(func() {
            newm(sysmon, nil, -1)
        })
    }

    lockOSThread()  // 初始化期间G锁在主要OS线程上

    runtimeInitTime = nanotime()   // 记下世界时间的时刻，须在doInit之前

    gcenable()  // 启动来及回收器

    // 按照依赖顺序运行所有模块的init任务
    last := lastmoduledatap

    unlockOSThread()

    fn := main_main // 间接调用：链接器此时不知道main包的地址
    fn()

    exit(0) // 主G一返回，整个进程退出


}

```

runtimeInitTime = nanotime()
